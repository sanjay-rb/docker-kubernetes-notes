name: Deploy Frontend
on:
  push:
    branches:
      - main
    paths:
      - "react-frontend/**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
        working-directory: react-frontend
      - run: docker build -t sanjoke/react-frontend -f Dockerfile.dev .
        working-directory: react-frontend
      - run: docker run -e CI=true sanjoke/react-frontend npm run test
        working-directory: react-frontend

      - name: Generate deployment package
        run: zip -r deploy.zip . -x '*.git*'
        working-directory: react-frontend

      - name: Deploy to EB
        uses: einaregilsson/beanstalk-deploy@v18
        with:
          aws_access_key: ${{ secrets.AWS_ACCESS_KEY }}
          aws_secret_key: ${{ secrets.AWS_SECRET_KEY }}
          application_name: react-frontend
          environment_name: React-frontend-env
          existing_bucket_name: elasticbeanstalk-us-east-1-162377439664
          region: us-east-1
          version_label: ${{ github.sha }}
          deployment_package: react-frontend/deploy.zip
